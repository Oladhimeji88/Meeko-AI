# Sounds

Drop alarm/timer sound files here. The app references `sounds/alarm.mp3` by
default (see `AlarmModel.soundAsset`).

Add your own royalty-free `alarm.mp3` to this folder before release. The app
degrades gracefully (vibration only) if the file is missing during development.
